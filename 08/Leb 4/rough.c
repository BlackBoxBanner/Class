#include<stdio.h>

int main()
{
    int num, x;
    printf("Enter the number : ");
    scanf("%d",&num);
    printf("The factor of %d is ", num);
    while (num == 1)
    {
        switch (x)
        {
        case 'num%2 == 0':
            printf("")
            break;
        
        default:
            break;
        }
    }
        

    return 0;
}
